# WeCircle — نظام التشغيل الموحد

# تشغيل Backend
cd dashboard/backend && npm run dev

# تشغيل Frontend (في terminal آخر)
cd dashboard/frontend && npm run dev

# تشغيل Flutter (في terminal آخر)
cd mobile && flutter run
